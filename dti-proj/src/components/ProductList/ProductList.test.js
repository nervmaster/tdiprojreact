import React from 'react';
import {cleanup, fireEvent, render} from '@testing-library/react';
import TopMenu from '.';
import { BrowserRouter as Router } from "react-router-dom";

/**
 * This unit test is not implemented because it needs some more study to do proper unit testing.
 * such as:
 *  - Mock the fetch requests (GET)
 */

describe('<ProductList />', () => {
    it('Should render successfully', () => {
    });

    it('Should display all products with edit button', () => {
    });

    it('Should show empty if no product', () => {
    });

    it('Should redirect for edit url of product id', () => {
    });
});